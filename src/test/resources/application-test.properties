spring.application.name=PermissionManager

# Configuración de la Base de Datos H2 para tests
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Configuración de JPA/Hibernate para tests
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Configuración de logging para tests
logging.level.org.springframework.security=WARN
logging.level.com.hconline.permissionmanager=WARN
logging.level.org.hibernate.SQL=INFO

# Configuración de JWT para tests
jwt.secret=estaesunaclavesecretaparaeljwtquetienemasde32caracteres
jwt.expiration=86400000
